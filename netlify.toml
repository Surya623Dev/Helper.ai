# Netlify build config
[build]
# Define your public folder if you use one, otherwise use "."
publish = "."

# Tell Netlify where to find your serverless functions
functions = "netlify/functions"

# Optional: Node bundler for functions
[functions]
# Use esbuild as the node bundler (recommended)
node_bundler = "esbuild"
